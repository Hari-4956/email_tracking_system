# Development (recommended): leave empty so Vite proxies /api, /health, /track
# to FastAPI at http://127.0.0.1:8000
VITE_API_BASE_URL=

# Production example (deployed FastAPI origin, no trailing slash):
# VITE_API_BASE_URL=https://api.example.com
